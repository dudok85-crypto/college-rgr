# **Расчётно-графическая работа №1**<br>Оценка сложности алгоритмов сортировки на языке программирования Python

**Актуальность темы:**<br>Эффективность работы является фундаментальной основой разработки программного обеспечения. При обработке больших объёмов данных выбор оптимального алгоритма напрямую влияет на быстродействие программ и потребление системных ресурсов. Язык программирования Python, благодаря своей выразительности и широкому распространению, требует от разработчиков понимания не только синтаксиса, но и асимптотической сложности применяемых алгоритмов.<br>

**Цель работы:** Изучить теоретические основы оценки сложности алгоритмов сортировки и провести эмпирические исследования их временной и пространственной сложности на языке программирования Python.

**Задача работы:** 
* 1 Изучить понятия временной и пространственной сложности алгоритмов.
* 2 Реализовать классические алгоритмы сортировки данных на Python.
* 3 Провести сравнительный теоретический анализ алгоритмов.
* 4 Выполнить практические замеры времени выполнения и потребления памяти с использованием библиотек `timeit`, `memory-profiler`, `tracemalloc`
* 5 Сопоставить теоретические оценки с практическими результатами и сформировать выводы.

# 1. Теоретическая часть.
## 1.1 Основные понятия оценки сложности алгоритмов
**Сложность алгоритма** - это функция, описывающая зависимость потребления ресурсов (времени выполнения или объёма памяти) от размера входных данных.

Выделяют два основных вида сложности:

1. **Временная сложность** (time Complexity) - Показывает как увеличивается время выполнения алгоритма при росте входных данных.
2. **Пространственная сложность** (Space Complexity) - Показывает какой объём оперативной памяти требуется алгоритму для работы по мере роста объёма входных данных.

Для описания сложности алгоритма в программировании используются асимптотические нотации, которые формируют следующие классы сложности.
* O(1) - Константная 
* O(n) - Линейная 
* O(n<sup>2</sup>) - Квадратическая 
* O(log<sub>n</sub>) - Логарифмическая
* O(n log<sub>n</sub>) - Линейно-логарифмическая

## 1.2 Алгоритмы сортировки и их реализация на Python

Для исследования выбраны четыре классических алгоритма сортировки данных: два - с квадратичной сложностью (пузырьковая сортировка и сортировка выбором), и два с линейно-логарифмической сложностью (быстрая сортировка и сортировка слиянием).

### 1.2.1 Пузырьковая сортировка. (Bubble Sort)

Алгоритм последовательно сравнивает соседние элементы массива и меняет их местами, если они стоят в неправильном порядке. Таким образом тяжёлые "элементы" постепенно перемещаются в конец массива.

```py
def Bubble_Sort(arr):
    # 1. Замер длины массива
    n = len(arr)
    # 2. Проход по всем элементам массива.
    for i in range(n):
        flag = False # Флаг: Были ли обмены на текущем проходе 
        # Последние i элементов были отсортированы, их трогать не нужно.
        for j in range(0, n-i-1):
            if arr[j]>arr[j+1]:
                # Меняем соседние элементы местами
                arr[j], arr[j+1] = arr[j+1], arr[j]
                flag = True
        # Если обменов не было, то массив отсортирован 
        if not flag:
            break

    return arr
```

Алгоритм прост в своей механике благодаря флагу `flag` в лучшем случае (уже отсортированный массив) работает по нотации линейного типа O(n). В худшем и среднем случае работает по нотации квадратической сложности (n<sup>2</sup>).<br>
Дополнительная память не требуется, сортировка выполняется "на месте", и таким образом пространственная сложность работает по константной нотации O(1).

### 1.2.2 Сортировка выбором (Selection Sort)

Алгоритм делит массив на отсортированную и неотсортированную части. На каждом шаге в неотсортированной части ищется минимальный элемент и перемещается в конец отсортированного.

```py
def Selection_Sort(arr):
    # 1. Замер длины массива
    n = len(arr)
     # 2. Проход по всем элементам массива.
    for i in range(n):
        min_index = i # Предполагаем, что текущий элемент минимальный.
        # Ищем реальный минимум в оставшейся части массива 
        for rel_min in range(0, i+1, n):
            if arr[rel_min]<arr[min_index]:
                min_index = rel_min
        # Меняем местами найденный минимум и текущий элемент 
        arr[i], arr[min_index] = arr[min_index], arr[i]

    return arr
```

Алгоритм всегда работает по нотации квадратической сложности (n<sup>2</sup>) независимо от начального порядка элементов. Память потребляется только для хранения нескольких индексов, потому пространственная сложность - O(n) не меняется.

### 1.2.3 Быстрая сортировка (Quick Sort)

Алгоритм, основанный на принципе "разделяй и властвуй": Выбирается опорный элемент (pivot), массив делится на элементы больше, равные и меньше опорного, после чего части сортируются рекурсивно.

```py
def Quick_Sort(arr):
    # 1. Замер длины массива
    n = len(arr)
    # Базовый случай рекурсии: Массив, состоящий из 0 или 1 элемента является отсортированным.
    if n<=1:
        return arr
    # 2. Сравнение элементов
    pivot = arr[n//2] # выбираем серединный элемент массива как опорник.
    left = [x for x in arr if x < pivot]    # Элементы меньше опорного
    right = [x for x in arr if x > pivot]       # Элементы больше опорного
    middle = [x for x in arr if x == pivot]     # Элементы равные опорному 

    # Рекурсивно сортируем левую и правую части и объединяем 
    return Quick_Sort(left) + middle + Quick_Sort(right)
```

Алгоритм работает с созданием новых списков - списки left, middle и right. Что увеличивает потребление памяти до O(n). Временная сложность оценивается по O(n log<sub>n</sub>) по нотации средней сложности, и по O(n<sup>2</sup>) в худших случаях.

### 1.2.4 Сортировка слиянием (Merge Sort)

Массив рекурсивно делится пополам до массивов, состоящих из 1 элемента. После чего происходит слияние отсортированных частей в единый массив.

```py
def Merge_Sort(arr):
    # 1. Определяем длину массива
    n = len(arr)
    if n<=1:
        return arr
    # 2. Определить середину массива для последующего его разделения.
    mid = n // 2
    # 3. Рекурсивно делим массив на две половины и сортируем.
    left_half = Merge_Sort(arr[:mid])
    right_half = Merge_Sort(arr[mid:])
    return merge(left_half, right_half)

def merge(left, right):
    result = []
    i = j = 0
    # Слияние двух отсортированных половин
    while i<len(left) and j<len(right):
        if left[i]<right[j]:
            result.append(left[i])
            i+=1
        else:
            result.append(right[j])
            j+=1
    # Добавляем оставшиеся элементы
    result.extend(left[i:])
    result.extend(right[j:])
    return result
```

Этот алгоритм гарантирует временную сложность линейно-логарифмического типа O(n log<sub>n</sub>) для любого набора данных. Однако он требует дополнительной памяти для промежуточных массивов, что по нотации определяет его пространственную сложность как O(n). 

## 1.3 Сравнительная таблица сложности алгоритмов сортировки данных.
|Алгоритм|Лучшее время|Среднее время|Худшее время|Память|Стабильность|
|-|-|-|-|-|-|
|Пузырьковая сортировка|O(n)|O(n<sup>2</sup>)|O(n<sup>2</sup>)|O(1)|Стабильное поведение|
|Сортировка выбором|O(n<sup>2</sup>)|O(n<sup>2</sup>)|O(n<sup>2</sup>)|O(log<sub>n</sub>)|Стабильное поведение|
|Быстрая сортировка|O(n log<sub>n</sub>)|O(n log<sub>n</sub>)|O(n<sup>2</sup>)|O(log<sub>n</sub>)|Нестабильное поведение|
|Сортировка слиянием|O(n log<sub>n</sub>)|O(n log<sub>n</sub>)|O(n log<sub>n</sub>)|O(n)|Стабильное|

# 2. Практическая часть
## 2.1 Инструменты исследования

Для проведения практических замеров используются специализированные библиотеки Python.
1. `timeit` - Встроенный модуль для точного измерения времени выполнения небольших фрагментов кода.
2. `tracemalloc` - Встроенный модуль для отслеживания выделений памяти, который позволяет отследить текущее и пиковое потребление памяти.
3. `memory-profiler` - сторонний модуль для построчного анализа потребления памяти, который позволяет детально оценить какие участки кода потребляют больше всего памяти.

## Исследование временной сложности с использованием `timeit`

```py
import random
import timeit

from sorts import Selection_Sort, Bubble_Sort, Quick_Sort, Merge_Sort

def measure_time(arr, sort_function, number):
    """Измерение времени выполнения с помощью библиотеки timeit
    
    Параметры функции:
    sort_function - функция сортировки 
    arr - массив данных
    number - количество повторений (для усреднения результата)

    Возвращает среднее время выполнения алгоритма в секундах.
    """
    def wrapper():
        """Функция, которая позволяет создавать копию массива перед его сортировкой
        для того, чтобы каждый запуск начинался с исходных значений.
        """
        arr_copy = arr.copy()
        sort_function(arr_copy)

    # Запускаем измерение 
    total_time = timeit.timeit(wrapper, number=number)
    # Возвращаем среднее время 
    return total_time / number

if __name__ == "__main__":
    # Генерация случайного списка
    test = [random.randint(1,1000) for _ in range(100000)]
    # Замеряем время для каждого алгоритма
    time_bubble = measure_time(test, Bubble_Sort, 10)
    time_selection = measure_time(test, Selection_Sort, 10)
    time_quick = measure_time(test, Quick_Sort, 10)
    time_merge = measure_time(test, Merge_Sort, 10)

    print(f'Сортировка пузырьком: {time_bubble}\nСортировка выбором: {time_selection}\nБыстрая сортировка: {time_quick}\nСортировка слиянием: {time_merge}')

```
